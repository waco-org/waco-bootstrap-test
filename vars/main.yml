---
# Workstation as Code
# Copyright Nicola Musatti 2025.

wabt_platform: "{{ (ansible_os_family == 'RedHat' and ansible_distribution != 'Fedora') |
ternary('EL' + ansible_distribution_major_version, ansible_distribution) }}"
